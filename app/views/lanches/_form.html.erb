<%= form_for @lanche, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :pessoa, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :pessoa, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :descricao, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :descricao, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit 'Fazer pedido', :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                lanches_path, :class => 'btn' %>
  </div>
<% end %>
